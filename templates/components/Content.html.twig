<div{{ attributes }}>
    <div class="vh-100 theme theme-{{ theme.value }}">
        <twig:Header key="{{ theme.value }}" seo="{{ seo }}" theme="{{ theme }}" />
        {% if seo.page == 'wave-function-collapse' %}
        <twig:WaveFunctionCollapse:Board />
        {% endif %}
        <twig:Footer />
    </div>
</div>
